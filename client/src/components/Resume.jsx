import React from 'react';
import axios from 'axios';
import {motion} from 'framer-motion'
const {useState} = React;


const Resume = (props) => {


  return(

    <motion.div id='experience' className='resume-container'>
      <motion.div className='resume-button'></motion.div>
    </motion.div>

    )

};

export default Resume;
